from datetime import datetime
from collections import defaultdict

# Time format in the log
TIME_FORMAT = "%H:%M:%S"

def parse_log_file(log_path):
    """
    Parses the log file and returns a dictionary of job start and end events.
    """
    pass